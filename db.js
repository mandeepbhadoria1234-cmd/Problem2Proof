const Database=require('better-sqlite3');const bcrypt=require('bcryptjs');const db=new Database('problem2proof.db');db.pragma('journal_mode = WAL');db.exec(`CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,email TEXT UNIQUE NOT NULL,password TEXT NOT NULL,role TEXT NOT NULL DEFAULT 'student',created_at DATETIME DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS reports(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,title TEXT NOT NULL,description TEXT NOT NULL,category TEXT,location TEXT,image TEXT,ai_analysis TEXT,severity INTEGER DEFAULT 1,reach INTEGER DEFAULT 1,evidence INTEGER DEFAULT 1,priority INTEGER DEFAULT 0,cluster_id INTEGER,department TEXT,status TEXT DEFAULT 'Submitted',proof_image TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id));CREATE TABLE IF NOT EXISTS notifications(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,message TEXT NOT NULL,read INTEGER DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id));`);if(!db.prepare('SELECT id FROM users WHERE email=?').get('admin@problem2proof.com')){db.prepare('INSERT INTO users(name,email,password,role) VALUES(?,?,?,?)').run('System Administrator','admin@problem2proof.com',bcrypt.hashSync('admin123',10),'admin')}module.exports=db;
