function analyzeReport(title,description){const t=`${title} ${description}`.toLowerCase();let category='Other',department='General Administration';if(/water|leak|tap|pipe/.test(t)){category='Water';department='Maintenance'}else if(/electric|light|fan|wire/.test(t)){category='Electrical';department='Electrical Department'}else if(/road|pothole|path/.test(t)){category='Road';department='Civil Department'}else if(/toilet|washroom|clean/.test(t)){category='Sanitation';department='Housekeeping'}else if(/wifi|internet|network/.test(t)){category='Internet';department='IT Department'}let severity=/danger|unsafe|broken|major/.test(t)?5:/urgent|serious/.test(t)?4:/problem|not working/.test(t)?3:1;const reach=3,evidence=3,priority=Math.round(severity*.5+reach*.3+evidence*.2);return{category,department,severity,reach,evidence,priority,analysis:`Detected category: ${category}. Recommended department: ${department}. Severity score: ${severity}/5.`}}module.exports={analyzeReport};
